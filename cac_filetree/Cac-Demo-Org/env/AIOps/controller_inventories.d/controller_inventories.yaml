---
controller_inventories:
  - name: AIOps-Localhost
    organization: Cac-Demo-Org
    description: Localhost inventory for AI intelligence workflow (MCP queries and playbook generation)
    variables:
      ansible_connection: local
      ansible_python_interpreter: "{{ '{{' }} ansible_playbook_python {{ '}}' }}"

controller_hosts:
  - name: localhost
    inventory: AIOps-Localhost
    variables:
      ansible_connection: local
